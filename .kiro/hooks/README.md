# Agent Hooks

훅은 Kiro IDE의 훅 UI에서 만드는 것이 가장 안전하다.
여기 있는 `.kiro.hook` 파일은 팀 공통 훅의 **초안**이며,
Kiro 버전에 따라 스키마가 다를 수 있으므로 최초 1회는
IDE에서 열어 정상 인식되는지 확인한 뒤 사용한다.

인식되지 않으면 파일을 지우고 UI에서 같은 내용으로 다시 만든 뒤 커밋한다.

훅을 커밋해 두면 팀원 5명 모두에게 동일한 품질 기준이 자동 적용된다.
