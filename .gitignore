# 비밀 정보
.env
.env.local
*.pem

# 외부 API 원본 응답 — 약관상 결과 데이터의 복제·저장·배포가 제한된다
# (ODsay 이용약관 4.5.10 / Google Maps Platform 약관).
# 로컬에서 참고용으로 두되 절대 커밋하지 말 것. 필드 구조는 docs/*_field_check.md 로 남긴다.
docs/*_sample_response.json
docs/**/api_response*.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Node
node_modules/
dist/
.vite/

# 데이터 (용량 큼 — 커밋하지 말 것)
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep

# OS / 에디터
.DS_Store
Thumbs.db
.idea/
.vscode/

# Kiro — 설정은 커밋하고, 로컬 캐시만 제외한다
.kiro/cache/
