# 협업 규칙

5명이 각자 Kiro 에이전트를 돌리면, 사람이 손으로 짤 때보다 머지 충돌이 훨씬 많이 난다.
에이전트는 태스크 하나에 파일 10개를 건드리기 때문이다. 아래 규칙은 그걸 막기 위한 것이다.

---

## 0. 최초 1회 세팅

```bash
git clone <리포 주소>
cd trip-planner
cp .env.example .env
```

Kiro로 이 폴더를 연 뒤, 채팅에 아래를 입력해 steering이 인식되는지 확인한다.

```
.kiro/steering 을 읽고, 내가 어느 경로를 소유하고 있는지 알려줘.
```

경로 소유권을 정확히 답하면 세팅 완료다. 못 하면 폴더를 잘못 열었거나 clone이 안 된 것이다.

---

## 1. 파일 소유권

`.kiro/steering/structure.md` 의 표가 기준이다. **내 경로 밖은 건드리지 않는다.**

다른 모듈이 잘못돼 보이면 직접 고치지 말고 담당자에게 말한다.
Kiro에게도 같은 규칙이 steering으로 적용되어 있다.

### 공용 파일 3종 — 별도 규칙

| 파일 | 규칙 |
|------|------|
| `shared/types/` | PM 단독 수정. 브랜치 쓰지 않고 main에 직접 푸시 후 전원 pull |
| `uv.lock` / `package-lock.json` | 의존성 추가는 **월·목 오전에만** 몰아서. 별도 커밋 |
| `backend/main.py` | router 등록 한 줄만 추가. 다른 부분 수정 금지 |

---

## 2. 브랜치

```
main                       항상 동작하는 상태. 직접 push 금지
 └── feat/scheduler-slot    1 spec = 1 브랜치
 └── fix/intake-null
```

- 브랜치 수명 **최대 2일**. 길어지면 spec을 덜 쪼갠 것이다.
- PR은 **300줄 이하** 목표. Kiro가 뱉는 대로 두면 2000줄이 나온다.
  `tasks.md` 의 태스크 2~3개 단위로 끊어서 커밋한다.
- 머지 전 반드시:

```bash
git pull --rebase origin main
# 로컬에서 실행 확인
```

- **매일 저녁 전원 main에 머지한다.** 하루 이상 묵히면 사람이 못 푸는 충돌이 된다.

## 3. 커밋 메시지

```
feat(scoring): POI 실패 확률 산정 로직 추가

type : feat | fix | docs | refactor | test | chore
scope: intake | scoring | planner | routing | share | frontend | data | kiro
```

## 4. 리뷰

- 리뷰어 1명, 30분 내 응답. 못 받으면 PM이 대신 승인한다.
- 리뷰는 "동작하는가"가 아니라 **"6주 뒤에 읽어도 이해되는가"** 를 본다.
  제출 코드 배점 10점이 여기에 걸려 있다.

---

## 5. Kiro 사용 규칙

- **spec 없이 큰 기능을 시키지 않는다.** 요구사항·설계·태스크를 먼저 만들고,
  사람이 검토한 뒤 구현을 시작한다.
- 에이전트가 소유 경로 밖 파일을 고치려 하면 **중단시킨다.**
- `.kiro/steering/` 은 어떤 경우에도 에이전트가 자동 수정하지 않게 한다.
- steering을 바꿔야겠다고 판단되면 팀 채팅에 올리고 PM이 반영한다.
- 프롬프트는 코드에 하드코딩하지 말고 `backend/common/prompts/` 에 파일로 둔다.

## 6. 충돌이 났을 때

```bash
git pull --rebase origin main
# 충돌 파일 확인
git status
```

- 충돌이 **3개 파일 이하**면 직접 해결한다.
- **4개 이상이거나 `shared/types/` 가 포함되면 혼자 풀지 말고** 팀 채팅에 올린다.
  잘못 푼 충돌은 조용히 기능을 망가뜨린다.
- 도저히 안 되면 `git rebase --abort` 후 브랜치를 새로 파고 변경분만 옮긴다.

---

## 7. 주간 리듬

| 시점 | 할 일 |
|------|-------|
| 월 스탠드업 | 이번 주 spec 배정, 브랜치 이름 확정 |
| 매일 저녁 | main 머지 (10분. 미룰수록 손해) |
| 수 비동기 | steering 위반 사례 공유 → steering 업데이트 |
| 금 통합일 | main에서 데모 실행. 통합 버그만 처리, 신규 기능 금지 |

## 8. 절대 하지 말 것

- `.env` 커밋 (CI가 막지만, 막히기 전에 키가 노출된다)
- main에 직접 push
- 데이터 원본 파일 커밋
- 남의 spec 폴더 수정
- 하루 넘게 브랜치 묵히기
