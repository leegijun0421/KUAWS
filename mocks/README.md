# 목 응답

**프론트엔드는 백엔드 완성을 기다리지 않는다.**
1주차에 확정한 `shared/types` 계약대로 만든 이 파일들로 UI를 먼저 만들고,
3주차에 실제 API로 교체한다. 이것만 지켜도 프론트 1명이 병목이 되지 않는다.

백엔드 담당자는 자기 모듈 구현이 끝나면 **실제 응답을 여기 파일에 덮어써서**
계약이 어긋나지 않았는지 확인한다.
